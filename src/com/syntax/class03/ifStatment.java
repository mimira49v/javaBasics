package com.syntax.class03;

public class ifStatment {

	public static void main(String[] args) {
		/*
		 * Scenario: I need to compare two numbers and i have to find
		 * which is bigger 
		 */
		int num1=100;
		int num2=50;
		// condition must be true to enter a block of code!!!
		if (num1>num2) {
			System.out.println(num1 + " is larger than " + num2);
			
		}
		//  if (num1<num2) {
			// System.out.println(num1+" is larger than "+num2);
			// false so it doesnt print anything
		/*
		 * Syntax of if statment
		 * if ( boolean condition) is true you can execute
		 * a block of code
		 */
	double money =5;
	double coffee=4.99;
	
	// if i have more money than a price of the coffe --> I am buying coffee
	
	if(money>=coffee) {
		
	
		System.out.println("I am buying the cofee");
		}
	
	//  if (money=coffee) { Error because if condition alwayss must be boolean we use 
	//  double --> because of the wrong operator 
	/* 
	 * if ( boolean condition) {
	 *     code;
	 * else {
	 *     code;    
	 */
	
	  
	
	/* 
	 * 
	 */
	
}

}